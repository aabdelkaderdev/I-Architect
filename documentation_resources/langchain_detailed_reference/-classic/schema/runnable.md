<!-- Source: https://reference.langchain.com/python/langchain-classic/schema/runnable -->

Modulev1.2.13 (latest)●Since v1.0

# runnable

LangChain **Runnable** and the **LangChain Expression Language (LCEL)**.

The LangChain Expression Language (LCEL) offers a declarative method to build
production-grade programs that harness the power of LLMs.

Programs created using LCEL and LangChain Runnables inherently support
synchronous, asynchronous, batch, and streaming operations.

Support for **async** allows servers hosting LCEL based programs to scale better
for higher concurrent loads.

**Streaming** of intermediate outputs as they're being generated allows for
creating more responsive UX.

This module contains schema and implementation of LangChain Runnables primitives.

## Modules

[module

fallbacks](/python/langchain-classic/schema/runnable/fallbacks)[module

base](/python/langchain-classic/schema/runnable/base)[module

utils](/python/langchain-classic/schema/runnable/utils)[module

history](/python/langchain-classic/schema/runnable/history)[module

branch](/python/langchain-classic/schema/runnable/branch)[module

passthrough](/python/langchain-classic/schema/runnable/passthrough)[module

config](/python/langchain-classic/schema/runnable/config)[module

retry](/python/langchain-classic/schema/runnable/retry)[module

configurable](/python/langchain-classic/schema/runnable/configurable)[module

router](/python/langchain-classic/schema/runnable/router)


