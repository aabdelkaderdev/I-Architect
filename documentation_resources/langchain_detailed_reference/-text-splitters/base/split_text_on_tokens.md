<!-- Source: https://reference.langchain.com/python/langchain-text-splitters/base/split_text_on_tokens -->

Functionv1.1.1 (latest)●Since v0.0

# split\_text\_on\_tokens

Split incoming text and return chunks using tokenizer.


```
split_text_on_tokens(
  *,
  text: str,
  tokenizer: Tokenizer
) -> list[str]
```

## Parameters

| Name | Type | Description |
| --- | --- | --- |
| `text`\* | `str` | The input text to be split. |
| `tokenizer`\* | `Tokenizer` | The tokenizer to use for splitting. |


