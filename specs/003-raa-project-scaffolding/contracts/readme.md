# Filesystem Layout Contract

The directory hierarchy and file locations under `raa/`, `Skills/RAA/`, and `embeddings/` must strictly adhere to the project directory layout specified in Section 21 of `RAA_Plan.md`.
Any deviations or movements must be approved by the core project architect to prevent build and path resolution breaks.
